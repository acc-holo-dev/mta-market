node_modules/
dist/
.next/
.turbo/
build/
*.log
.env
.env.local
.DS_Store
*.tsbuildinfo
uploads/
backups/
ssl/
.env.*
!.env.example
apps/server/.agents/
apps/server/.claude/
apps/server/.cursor/
apps/server/.devin/
# PLAN-004 C-003 (audit G-03): migration snapshots MUST be versioned — the
# committed refs/db.json points at a snapshot hash; ignoring this directory
# left a dangling ref on fresh clones (MIGRATION.SNAPSHOT_MISSING) and broke
# schema reproducibility.
# apps/server/migrations/

# Playwright
test-results/
playwright-report/
